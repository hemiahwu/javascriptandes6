const num1 = 100;
const num2 = 50;

let val;

// 数值运算
// val = num1 + num2;
// val = num1 - num2;
// val = num1 * num2;
// val = num1 / num2;
// val = num1 % num2;

// Math Object
// val = Math.PI;
// val = Math.round(2.6); // 四舍五入
// val = Math.ceil(2.4); // 向上取整
// val = Math.floor(2.8);   // 向下取整
// val = Math.sqrt(64); // 平方
// val = Math.abs(-3); // 取绝对值
// val = Math.min(2, 33, 4, 1, 55, 6, 3, -2); // 最小值
// val = Math.max(2, 33, 4, 1, 55, 6, 3, -2); // 最大值

// val = Math.random(); // 随机数
val = Math.floor(Math.random() * 20 + 1);

console.log(val);
